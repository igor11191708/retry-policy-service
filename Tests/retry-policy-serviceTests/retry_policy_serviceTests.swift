import XCTest
@testable import retry_policy_service

final class retry_policy_serviceTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(retry_policy_service().text, "Hello, World!")
    }
}
